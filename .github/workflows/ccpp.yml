name: C/C++ CI

on:
  push:
    paths:
    - 'AwakeMUD/**'

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: make
        working-directory: AwakeMUD
        run: make
